# 📐 SMASH OR PASS - Architecture & Tech Stack

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT LAYER                              │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  React SPA (Vite)                                        │   │
│  │  ├── AuthScreen (Login/Register)                        │   │
│  │  ├── DiscoverScreen (Swipe cards)                       │   │
│  │  ├── ChatScreen (Real-time messages)                    │   │
│  │  ├── MessagesScreen (Inbox)                             │   │
│  │  ├── ProfileScreen (User profile)                       │   │
│  │  └── MatchScreen (Celebration)                          │   │
│  │                                                          │   │
│  │  TailwindCSS + Material Icons                           │   │
│  └──────────────────────────────────────────────────────────┘   │
│         │                                    │                    │
│         │ REST API (fetch)                   │ WebSocket (io)    │
│         │ + JWT Token                        │ Real-time         │
└─────────┼────────────────────────────────────┼────────────────────┘
          │                                    │
          ▼                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                     APPLICATION LAYER                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Node.js + Express.js Server                            │   │
│  │                                                          │   │
│  │  ┌────────────────────────────────────────────────────┐ │   │
│  │  │  REST API Routes                                 │ │   │
│  │  │  ├── /auth/*          (register, login)          │ │   │
│  │  │  ├── /users/*         (profile, discover)        │ │   │
│  │  │  ├── /swipes/*        (like, pass)               │ │   │
│  │  │  ├── /conversations/* (messages)                 │ │   │
│  │  │  └── /reports/*       (safety)                   │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  │                                                          │   │
│  │  ┌────────────────────────────────────────────────────┐ │   │
│  │  │  WebSocket (Socket.io)                           │ │   │
│  │  │  ├── message:send                                │ │   │
│  │  │  ├── typing:start/stop                           │ │   │
│  │  │  ├── message:read                                │ │   │
│  │  │  ├── message:react                               │ │   │
│  │  │  └── match:notify                                │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  │                                                          │   │
│  │  Middleware:                                            │   │
│  │  ├── Auth (JWT verification)                          │   │
│  │  ├── CORS                                             │   │
│  │  ├── Rate Limiting                                    │   │
│  │  └── Error Handling                                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│         │                                                         │
└─────────┼─────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATA LAYER                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  MongoDB (NoSQL)                                         │   │
│  │  Collections:                                            │   │
│  │  ├── users        (5 indexes: email, location)          │   │
│  │  ├── matches      (indexes: user1, user2)               │   │
│  │  ├── messages     (indexes: conversation, sender)       │   │
│  │  ├── conversations(indexes: participants)               │   │
│  │  ├── swipes       (indexes: user, target)               │   │
│  │  └── reports      (indexes: status)                     │   │
│  │                                                          │   │
│  │  Replication: Automatic backup (Atlas)                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  External Services                                       │   │
│  │  ├── Cloudinary (Image hosting)                         │   │
│  │  ├── SendGrid/Resend (Email)                            │   │
│  │  └── (optional) AWS S3                                  │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 🔀 Data Flow Example - User Swiping

```
User swipes → Browser sends HTTP POST /api/swipes
    ↓
Backend validates JWT token
    ↓
Database checks for mutual like
    ↓
IF match → Create conversation + WebSocket "match:new" event
    ↓
Browser receives event → Show match animation
    ↓
User can message → Click "Send a Message"
    ↓
Browser sends WebSocket "message:send"
    ↓
Server broadcasts to recipient via WebSocket
    ↓
Chat updates in real-time
```

## 📦 Technology Stack

### Frontend
```
┌─ React 18
│  ├─ Hooks (useState, useEffect, useRef)
│  ├─ Context API (state management)
│  └─ Component-based architecture
│
├─ Vite 6 (build tool)
│  ├─ Hot Module Replacement
│  ├─ Lightning-fast builds
│  └─ Optimized bundling
│
├─ TailwindCSS 3
│  ├─ Utility-first CSS
│  ├─ Dark mode support
│  └─ Custom theme config
│
├─ Socket.io-client
│  └─ Real-time WebSocket
│
└─ Modern JavaScript (ES6+)
   ├─ Fetch API
   └─ Async/await
```

### Backend
```
┌─ Node.js 20 LTS
│  └─ JavaScript runtime
│
├─ Express.js 4.18
│  ├─ HTTP routing
│  ├─ Middleware
│  └─ Request handling
│
├─ Socket.io 4.7
│  ├─ WebSocket abstraction
│  ├─ Event-based messaging
│  └─ Automatic reconnection
│
├─ MongoDB 7.0
│  ├─ NoSQL database
│  ├─ Document-based
│  └─ Geospatial queries
│
├─ Mongoose 8.0
│  ├─ ODM (Object Document Mapper)
│  ├─ Schema validation
│  └─ Model creation
│
├─ Authentication
│  ├─ bcryptjs (password hashing)
│  └─ jsonwebtoken (JWT)
│
├─ Security
│  ├─ helmet (HTTP headers)
│  ├─ cors (cross-origin)
│  └─ express-rate-limit
│
└─ Utilities
   ├─ dotenv (env variables)
   ├─ multer (file upload)
   ├─ joi (validation)
   └─ lodash (helpers)
```

### DevOps & Deployment
```
┌─ Docker
│  ├─ Container images
│  ├─ Multi-stage builds
│  └─ Consistent environments
│
├─ Docker Compose
│  ├─ Local development
│  ├─ Service orchestration
│  └─ Network configuration
│
├─ GitHub Actions
│  ├─ CI/CD pipeline
│  ├─ Automated testing
│  └─ Auto-deployment
│
├─ Hosting Options
│  ├─ Railway (easy)
│  ├─ Vercel + Render
│  └─ AWS (advanced)
│
└─ Monitoring
   ├─ CloudWatch (AWS)
   ├─ Railway logs
   └─ Error tracking
```

## 🗄️ Database Schema Visualization

```
┌─────────────────┐
│     Users       │
├─────────────────┤
│ _id             │◄────────┐
│ email*          │         │
│ password        │         │
│ firstName       │         │
│ lastName        │         │
│ age             │         │
│ gender          │         │
│ avatar          │         │
│ photos[]        │         │
│ bio             │         │
│ tags[]          │         │
│ location{       │         │
│   type, coords  │         │
│ }               │         │
│ verified        │         │
│ online          │         │
│ blocked[]       │         │
│ reported[]      │         │
│ createdAt       │         │
│ updatedAt       │         │
└─────────────────┘         │
        ▲                   │
        │                   │
        │                   ▼
        │          ┌──────────────────┐
        │          │     Matches      │
        │          ├──────────────────┤
        │          │ _id              │
        │          │ user1────────────┼───────┬─────────┐
        │          │ user2────────────┼───────┤         │
        │          │ mutualLike       │       │         │
        │          │ matchedAt        │       │         │
        │          │ conversation──────────┐  │         │
        │          │ blocked          │    │  │         │
        └──────────┼──────────────────┘    │  │         │
                   │                        │  │         │
                   │                        ▼  │         │
                   │           ┌──────────────────────┐  │
                   │           │  Conversations       │  │
                   └───────────►├──────────────────────┤  │
                                │ _id                 │  │
                                │ participants[]◄─────┴──┴─┐
                                │ lastMessage────┐         │
                                │ lastMessageText│         │
                                │ unreadCount    │         │
                                │ createdAt      │         │
                                │ updatedAt      │         │
                                └──────┬─────────┘         │
                                       │                  │
                                       ▼                  │
                                ┌──────────────────┐      │
                                │    Messages      │      │
                                ├──────────────────┤      │
                                │ _id              │      │
                                │ conversation──────┐     │
                                │ sender───────────────────┘
                                │ text             │
                                │ image            │
                                │ reaction         │
                                │ read             │
                                │ readAt           │
                                │ createdAt        │
                                └──────────────────┘

┌──────────────────┐
│     Swipes       │
├──────────────────┤
│ _id              │
│ user────────────►│
│ target──────────►│
│ action           │ ('like' | 'pass')
│ createdAt        │
└──────────────────┘

┌──────────────────┐
│     Reports      │
├──────────────────┤
│ _id              │
│ reportedBy──────►│
│ reportedUser────►│
│ reason           │ (enum)
│ description      │
│ status           │ ('pending')
│ createdAt        │
└──────────────────┘
```

## 🔄 API Flow Diagram

```
                    ┌─────────────────────────────┐
                    │      Frontend (React)       │
                    └──────────────┬──────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                    ▼                             ▼
            ┌────────────────┐          ┌──────────────────┐
            │  REST API      │          │  WebSocket       │
            │  (HTTP)        │          │  (Real-time)     │
            └────────┬───────┘          └────────┬─────────┘
                     │                           │
        ┌────────────┼──────────────┬────────────┼─────────────┐
        │            │              │            │             │
        ▼            ▼              ▼            ▼             ▼
    ┌─────────┐ ┌────────┐ ┌──────────┐ ┌────────────┐ ┌──────────┐
    │  Auth   │ │ Users  │ │ Swipes   │ │ Messages   │ │ Rooms    │
    │/register│ │/profile│ │/swipes   │ │ /send      │ │ /join    │
    │/login   │ │/feed   │ │          │ │ /read      │ │ /leave   │
    └─────────┘ └────────┘ └──────────┘ └────────────┘ └──────────┘
        │            │              │            │             │
        └────────────┼──────────────┼────────────┼─────────────┘
                     │
        ┌────────────▼──────────────┐
        │    Express Router         │
        │    ├─ Validation (Joi)    │
        │    ├─ Auth Middleware     │
        │    ├─ Rate Limiting       │
        │    └─ Error Handling      │
        └────────────┬──────────────┘
                     │
        ┌────────────▼──────────────────────────────────┐
        │    MongoDB (Mongoose Models)                 │
        │    ├─ User                                   │
        │    ├─ Match                                  │
        │    ├─ Message                                │
        │    ├─ Conversation                           │
        │    ├─ Swipe                                  │
        │    └─ Report                                 │
        └────────────┬──────────────────────────────────┘
                     │
        ┌────────────▼──────────────┐
        │  MongoDB Atlas Database   │
        │  ├─ Collections (6)       │
        │  ├─ Indexes (10+)         │
        │  ├─ Backup (daily)        │
        │  └─ Replication (3x)      │
        └───────────────────────────┘
```

## 🔐 Authentication Flow

```
┌──────────────┐
│   Register   │
└──────┬───────┘
       │
       ▼
┌────────────────────────┐
│ POST /auth/register    │
│ - Validate input       │
│ - Check email exists   │
│ - Hash password        │
└──────┬─────────────────┘
       │
       ▼
┌────────────────────────┐
│ Save User to MongoDB   │
│ Create JWT token       │
└──────┬─────────────────┘
       │
       ▼
┌────────────────────────┐
│ Return token + user    │
│ Store in localStorage  │
└──────┬─────────────────┘
       │
       ▼
┌────────────────────────┐
│   Login (Subsequent)   │
│ 1. Check email exists  │
│ 2. Compare passwords   │
│ 3. Generate token      │
│ 4. Return token        │
└──────┬─────────────────┘
       │
       ▼
┌────────────────────────┐
│ All API requests:      │
│ Header: Authorization  │
│ "Bearer [token]"       │
│ Verify with secret key │
└────────────────────────┘
```

## 🎯 Request-Response Example

```
Frontend Request:
─────────────────────────────────────
POST /api/swipes
Headers: {
  "Authorization": "Bearer eyJhbGc...",
  "Content-Type": "application/json"
}
Body: {
  "targetId": "507f1f77bcf86cd799439011",
  "action": "like"
}

Backend Response:
─────────────────────────────────────
200 OK
{
  "success": true,
  "data": {
    "match": true,
    "conversation": "507f1f77bcf86cd799439012"
  }
}

WebSocket Event (Real-time):
─────────────────────────────────────
io.emit('match:new', {
  matchedWith: "user123",
  timestamp: 1630000000000
})
```

## 📈 Scalability

```
Development        Single Server       Scaled
───────────────────────────────────────────────────
Frontend           React SPA           CDN + S3
Backend            Node/Express        Load Balancer
Database           MongoDB             Sharded Cluster
Cache              -                   Redis
WebSocket          Socket.io           Redis Pub/Sub
File Storage       Local               AWS S3
Monitoring         -                   CloudWatch
Users              1-10                1000+
RPS                1-10                100+
```

This architecture can scale from 0 to 1M+ users with minimal changes!
